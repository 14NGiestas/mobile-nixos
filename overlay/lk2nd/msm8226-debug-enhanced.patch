diff --git a/app/aboot/aboot.c b/app/aboot/aboot.c
index a451443..cadc022 100644
--- a/app/aboot/aboot.c
+++ b/app/aboot/aboot.c
@@ -95,6 +95,94 @@
 #include <menu_keys_detect.h>
 #include <display_menu.h>
 #include "fastboot_test.h"
+#include <vibrator.h>
+
+/* Debug checkpoint macros - simple vibration streams with 2 second pauses between */
+#define DEBUG_CHECKPOINT_0() \
+	do { \
+		vib_timed_turn_on(2000); \
+		mdelay(2000); \
+		mdelay(2000); \
+		dprintf(INFO, "lk2nd: CP0\n"); \
+	} while(0)
+
+/* Rapid pulse stream - rapid short timed vibrations for N pulses */
+#define DEBUG_CHECKPOINT_1() \
+	do { \
+		int i; \
+		for (i = 0; i < 2; i++) { \
+			vib_timed_turn_on(50); \
+			mdelay(100); \
+		} \
+		mdelay(2000); \
+		dprintf(INFO, "lk2nd: CP1\n"); \
+	} while(0)
+
+#define DEBUG_CHECKPOINT_2() \
+	do { \
+		int i; \
+		for (i = 0; i < 3; i++) { \
+			vib_timed_turn_on(50); \
+			mdelay(100); \
+		} \
+		mdelay(2000); \
+		dprintf(INFO, "lk2nd: CP2\n"); \
+	} while(0)
+
+#define DEBUG_CHECKPOINT_3() \
+	do { \
+		int i; \
+		for (i = 0; i < 4; i++) { \
+			vib_timed_turn_on(50); \
+			mdelay(100); \
+		} \
+		mdelay(2000); \
+		dprintf(INFO, "lk2nd: CP3\n"); \
+	} while(0)
+
+#define DEBUG_CHECKPOINT_4() \
+	do { \
+		int i; \
+		for (i = 0; i < 5; i++) { \
+			vib_timed_turn_on(50); \
+			mdelay(100); \
+		} \
+		mdelay(2000); \
+		dprintf(INFO, "lk2nd: CP4\n"); \
+	} while(0)
+
+#define DEBUG_CHECKPOINT_5() \
+	do { \
+		int i; \
+		for (i = 0; i < 6; i++) { \
+			vib_timed_turn_on(50); \
+			mdelay(100); \
+		} \
+		mdelay(2000); \
+		dprintf(INFO, "lk2nd: CP5\n"); \
+	} while(0)
+
+#define DEBUG_CHECKPOINT_6() \
+	do { \
+		int i; \
+		for (i = 0; i < 7; i++) { \
+			vib_timed_turn_on(50); \
+			mdelay(100); \
+		} \
+		mdelay(2000); \
+		dprintf(INFO, "lk2nd: CP6\n"); \
+	} while(0)
+
+#define DEBUG_CHECKPOINT_7() \
+	do { \
+		int i; \
+		for (i = 0; i < 8; i++) { \
+			vib_timed_turn_on(50); \
+			mdelay(100); \
+		} \
+		mdelay(2000); \
+		dprintf(INFO, "lk2nd: CP7\n"); \
+	} while(0)
 
 #if WITH_LK2ND
 #include <lk2nd/init.h>
@@ -5496,7 +5584,10 @@ void aboot_init(const struct app_descriptor *app)
 	/* Initialise wdog to catch early lk crashes */
 #if WDOG_SUPPORT
 	msm_wdog_init();
-#endif
+	#endif
+
+	/* CP0: 2 second long vibration to mark start */
+	DEBUG_CHECKPOINT_0();
 
 	/* Setup page size information for nv storage */
 	if (target_is_emmc_boot())
@@ -5513,6 +5604,9 @@ void aboot_init(const struct app_descriptor *app)
 	}
 	ASSERT((MEMBASE + MEMSIZE) > MEMBASE);
 
+	/* CP1: 2 pulses */
+	DEBUG_CHECKPOINT_1();
+
 #if !ABOOT_STANDALONE
 	read_device_info(&device);
 	read_allow_oem_unlock(&device);
@@ -5520,6 +5614,9 @@ void aboot_init(const struct app_descriptor *app)
 	device.is_unlocked = true;
 #endif
 
+	/* CP2: 3 pulses */
+	DEBUG_CHECKPOINT_2();
+
 	/* Detect multi-slot support */
 	if (partition_multislot_is_supported())
 	{
@@ -5592,6 +5689,9 @@ void aboot_init(const struct app_descriptor *app)
 	}
 	if (!boot_into_fastboot)
 	{
+		/* CP3: 4 pulses */
+		DEBUG_CHECKPOINT_3();
+
 		if (keys_get_state(KEY_VOLUMEUP))
 			boot_into_recovery = 1;
 		if (!boot_into_recovery &&
@@ -5603,6 +5703,9 @@ void aboot_init(const struct app_descriptor *app)
 		boot_into_fastboot = true;
 	#endif
 
+	/* CP4: 5 pulses */
+	DEBUG_CHECKPOINT_4();
+
 #if USE_PON_REBOOT_REG
 	reboot_mode = check_hard_reboot_mode();
 #else
@@ -5650,9 +5753,15 @@ void aboot_init(const struct app_descriptor *app)
 	dprintf(INFO, "Fastboot mode was forced with compile-time flag.\n");
 #endif
 
+	/* CP5: 6 pulses */
+	DEBUG_CHECKPOINT_5();
+
 normal_boot:
 	if (!boot_into_fastboot)
 	{
+		/* CP6: 7 pulses */
+		DEBUG_CHECKPOINT_6();
+
 #if WITH_LK2ND_BOOT
 		if (!boot_into_recovery)
 			lk2nd_boot();
@@ -5676,16 +5785,19 @@ normal_boot:
 			}
 
 retry_boot:
-			/* Trying to boot active partition */
-			if (partition_multislot_is_supported())
-			{
-				boot_slot = partition_find_boot_slot();
-				partition_mark_active_slot(boot_slot);
-				if (boot_slot == INVALID)
-					goto fastboot;
-			}
+		/* Trying to boot active partition */
+		if (partition_multislot_is_supported())
+		{
+			boot_slot = partition_find_boot_slot();
+			partition_mark_active_slot(boot_slot);
+			if (boot_slot == INVALID)
+				goto fastboot;
+		}
+
+		/* CP7: 8 pulses */
+		DEBUG_CHECKPOINT_7();
 
-			boot_err_type = boot_linux_from_mmc();
+		boot_err_type = boot_linux_from_mmc();
 			switch (boot_err_type)
 			{
 				case ERR_INVALID_PAGE_SIZE:
